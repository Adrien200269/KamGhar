/**
 * Geospatial helper utilities for KamGhar
 */

/**
 * Calculates straight-line distance in kilometers using the Haversine formula
 * Useful for fast client-side sorting or fallback calculations.
 */
export function calculateHaversineDistanceKm(
  lat1: number,
  lon1: number,
  lat2: number,
  lon2: number
): number {
  const R = 6371; // Earth radius in km
  const dLat = ((lat2 - lat1) * Math.PI) / 180;
  const dLon = ((lon2 - lon1) * Math.PI) / 180;
  const a =
    Math.sin(dLat / 2) * Math.sin(dLat / 2) +
    Math.cos((lat1 * Math.PI) / 180) *
      Math.cos((lat2 * Math.PI) / 180) *
      Math.sin(dLon / 2) *
      Math.sin(dLon / 2);
  const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
  return Number((R * c).toFixed(2));
}

/**
 * Formats a distance in kilometers or meters for UI display
 */
export function formatDistance(distanceKm: number): string {
  if (distanceKm < 1) {
    return `${Math.round(distanceKm * 1000)} m away`;
  }
  return `${distanceKm.toFixed(1)} km away`;
}

/**
 * Kathmandu Valley default center coordinates (useful as fallback for initial launch)
 */
export const DEFAULT_MAP_CENTER = {
  lat: 27.7172,
  lng: 85.324,
  name: "Kathmandu, Nepal",
};
